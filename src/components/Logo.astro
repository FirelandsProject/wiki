---
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
const base = Astro.site?.origin === 'https://firelands-core.github.io' ? '/wiki' : '';
---

<img
	width="60"
	height="60"
	src={`${base}/logo.png`}
	class={className}
	alt="Firelands Logo"
/>